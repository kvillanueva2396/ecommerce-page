import { css } from 'lit'

const styles = css`
	.ecommerce-badge {
		font-family: var(--font-family-primary);
		font-weight: 700;
		background: var(--orange);
		color: var(--white);
		display: flex;
		justify-content: center;
		border-radius: 30%;
		font-size: 10px;
		width: 20px;
		padding: 1px 0;
		position: absolute;
		top: 0;
		right: 0;
	}
`

export default styles
